"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fixDecimals = exports.addition = void 0;
/**
 *
 * @param items items to apply addition operation
 * @param digits digits for precision
 */
function addition(items, digits) {
    if (digits === void 0) { digits = 2; }
    var total = 0;
    items.forEach(function (item) {
        total += parseFloat(item.toString()) * Math.pow(10, digits);
    });
    return total.toString();
}
exports.addition = addition;
/**
 *
 * @param { number | String } value number to fix
 */
function fixDecimals(value, decimals) {
    if (decimals === void 0) { decimals = 2; }
    value = parseFloat(value.toString());
    if (Number.isNaN(value)) {
        return '0.00';
    }
    var _value = value * Math.pow(10, decimals);
    var intPart = parseInt(value.toString(), 10);
    var decimalPart = parseInt((_value % Math.pow(10, decimals)).toString(), 10)
        .toString()
        .padEnd(2, '0');
    return intPart + '.' + decimalPart;
}
exports.fixDecimals = fixDecimals;
